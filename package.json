{
  "name": "live-web-app",
  "version": "1.0.0",
  "main": "server/scr/server.js",
  "scripts": {
    "build": "cd server/scr && npm install && npm run build",
    "start": "node server/scr/server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.0.0",
    "vite": "^4.4.9"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
