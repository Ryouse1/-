{
  "name": "live-web",
  "version": "1.0.0",
  "description": "Real-time Live Web project",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "cd frontend && npm install && npm run build",
    "install:all": "npm install && cd frontend && npm install"
  },
  "author": "Ryouse1",
  "license": "MIT",
  "dependencies": {
    "express": "^4.19.2",
    "socket.io": "^4.7.5",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "body-parser": "^1.20.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
